# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib


# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Build binary name
builder/*
!builder/.gitkeep
!builder/derp_admit/
builder/derp_admit/*
!builder/derp_admit/Dockerfile
!builder/derp_admit/docker-compose.yml
/.build/
# Created by .ignore support plugin (hsz.mobi)
/.idea/
*.iml
vendor/*

# Mirror scripts downloaded at runtime
/scripts/mirror/main.sh
/scripts/mirror/docker.sh
